server {
    listen 8080;

    access_log off;
    error_log  /var/log/nginx/error.log warn;

    location = /healthz {
        default_type text/plain;
        return 200 "alive\n";
    }

    location / {
        default_type text/plain;
        return 200 "greentic ingress stub\n";
    }
}
